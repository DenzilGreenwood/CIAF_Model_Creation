---
apiVersion: v1
kind: Service
metadata:
  name: ciaf-frontend
  namespace: ciaf
  labels:
    app: ciaf-frontend
    component: frontend
    tier: web
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: ciaf-frontend
